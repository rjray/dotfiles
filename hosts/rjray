# Stuff specific to work desktop
export PATH=/sbin:/usr/sbin:$PATH

# Incompatibilities with Python libraries:
post-review() {
    cwd=$(pwd)
    ssh cyclsvl "cd $cwd; /usr/software/rats/bin/post-review $*"
}
