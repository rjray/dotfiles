# Aliases specific to gangrel
backuphome () {
    rsync -e ssh -auzv --exclude '/Dropbox' --exclude '.Trash' --exclude '.mozilla/firefox/*/Cache' --exclude '/.cache' /home/users/rjray/ /freeagent0/backups/tremere/rjray
}
