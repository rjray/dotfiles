# Aliases specific to gangrel
backuphome () {
    rsync -e ssh -auzv --delete --exclude-from $HOME/.rsync-exclude /home/users/rjray/ tremere:/freeagent0/backups/gangrel/rjray
}

mtremere () {
    grep tremere /etc/fstab | awk '{print $1}' | xargs -n 1 -r mount
}

umtremere () {
    df | grep tremere: | awk '{print $1}' | xargs -n 1 -r umount
}

# Env-vars we don't want in .bash_env
export AUTHOR_TESTING=1

# Specific PATH elements
export PATH=$PATH:/opt/android-sdk-linux_86/tools
